'use strict';

app.controller('uploadImageController', uploadImageController);

uploadImageController.$inject = ['$rootScope', '$routeParams', '$scope', 'userService', 'authProvider', 'constantFactory', '$location'];

function uploadImageController($rootScope, $routeParams, $scope, userService, authProvider, constantFactory, $location) {
    console.log('entering uploadImageController');



}